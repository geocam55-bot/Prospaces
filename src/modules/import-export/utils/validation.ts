// Utilities for validation engine

export function validateRows(rows: any[], schema: any): { errors: any[]; warnings: any[] } {
  // TODO: Implement required fields, types, duplicates, referential integrity
  return { errors: [], warnings: [] };
}
